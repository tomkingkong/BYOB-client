language: node_js
node_js:
  - 'stable'
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run build
deploy:
  provider: heroku
  app: ml-tk-sellers-cellars
  skip_cleanup: true
  on:
    branch: master
